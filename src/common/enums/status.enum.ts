export enum statusEnum {
    PENDING = "pending",
    ACCEPTED = "accepted",
    REJECTED = "rejected",
    DISQUALIFICATION = "disqualification"
}
export enum AppointmentStatusEnum {
    reserved = "reserved",
    pending = "pending",
    done = "done",
    canceled = "canceled"
}